# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Supabase Database (PostgreSQL)
# Get password from: https://supabase.com/dashboard/project/YOUR-PROJECT/settings/database
# Connection Pooling (use for serverless - Vercel)
DATABASE_URL="postgresql://postgres.YOUR-PROJECT-REF:[YOUR-PASSWORD]@aws-1-REGION.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct Connection (use for migrations)
DIRECT_URL="postgresql://postgres.YOUR-PROJECT-REF:[YOUR-PASSWORD]@aws-1-REGION.pooler.supabase.com:5432/postgres"

# Supabase API
NEXT_PUBLIC_SUPABASE_URL="https://YOUR-PROJECT-REF.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"

# PartyKit (Real-time Collaboration)
# Get from: npx partykit deploy (after login)
NEXT_PUBLIC_PARTYKIT_HOST="your-project-name.your-username.partykit.dev"

# Feature Flags
# Cursor Nodes: Render cursors as React Flow nodes (true) or overlay (false)
# Set to "true" to enable new implementation (better performance, zero lag)
NEXT_PUBLIC_USE_CURSOR_NODES="false"
